---
description: Systematic four-phase debugging — reproduce → isolate → fix → verify.
---

# /debug

Read `.agent/skills/systematic-debugging/SKILL.md` and follow it exactly.

Do NOT propose fixes before completing Phase 1 (reproducing the bug with a failing test or deterministic repro). Speculation is not debugging — evidence is.

If the user has only described symptoms, ask the questions the skill specifies before starting Phase 1.
